'use client';
import React from 'react';
import type { SourceBreakdown } from '@/lib/metrics';
/** Placeholder — replaced by the Phase E filterable per-source table. */
export const SourceBreakdownTable: React.FC<{ sources: SourceBreakdown[] }> = () => null;
